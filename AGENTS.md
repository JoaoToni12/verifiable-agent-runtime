# Repository instructions

- Keep this repository independent of all employer systems and terminology.
- Use only synthetic examples and public interfaces.
- Preserve immutable request and event models where practical.
- Keep policy decisions deterministic and test them before orchestration code.
- Run `make check` before reporting completion.
- Never add real credentials, personal data, production URLs, or copied Git history.
- Do not publish or create a remote without explicit user authorization.
